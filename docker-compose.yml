version: '1'

services:
  flask-app:
    build: .
    container_name: flask_app
    ports:
      - "5000:5000"
    env_file:
      - .env
    depends_on:
      - mysql

  mysql:
    image: mysql:5.7
    container_name: mysql_db
    env_file:
      - .env
    ports:
      - "3306:3306"